import { VinReqList } from '@/components/ui';
// import { useNavigate } from 'react-router-dom';

const VinRequestListPage = () => {
  // const navigate = useNavigate();

  return <VinReqList />;
};

export default VinRequestListPage;
